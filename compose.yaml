services:
  frontend:
    container_name: frontend
    build:
      context: ./frontend
    ports:
      - "3000:3000"

  backend:
    container_name: backend
    build:
      context: ./backend
    ports:
      - "8080:8080"
